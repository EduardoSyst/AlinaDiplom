class Passenger:
    # Атрибуты: id, arrival_time, stop_id, boarding_time, exit_stop
    
class Bus:
    # Атрибуты: id, release_time, current_stop, passengers[]
    
class Stop:
    # Атрибуты: id, lambda_arrival, lambda_exit, passengers_waiting[]